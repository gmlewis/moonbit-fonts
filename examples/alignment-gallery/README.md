# Alignment Gallery

This example shows the same text rendered with all 12 possible `Alignment` values.
The red cross indicates the origin `(0,0)` relative to the text.

![Alignment Gallery](alignment-gallery.svg)

## How to run
```bash
cd examples/alignment-gallery
moon run . > alignment-gallery.svg
```

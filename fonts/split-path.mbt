/// `Cmd` represents an SVG command along with its parameters.
struct Cmd {
  c : String
  p : Params
}

/// `split_path` splits an SVG command into an array of individual commands.
fn split_path(d : String) -> Array[Cmd] {
  let cmds = []
  // TODO
  cmds
}
